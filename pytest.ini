[pytest]
# auto mode: `async def test_*` functions run under pytest-asyncio without
# needing a per-test @pytest.mark.asyncio marker. Guards against the
# silent false-pass where an un-awaited coroutine test "passes" trivially.
asyncio_mode = auto
testpaths = tests
